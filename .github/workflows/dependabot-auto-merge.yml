name: Dependabot Auto-merge

on:
  pull_request:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    name: Auto-merge patch/minor updates
    if: github.event.pull_request.head.repo.full_name == github.repository
    uses: dinglebear-ai/workflows/.github/workflows/dependabot-automerge.yml@ac57c3208cf92d71c5971bb936df51c400cb1ccf
    with:
      allowed-update-types: version-update:semver-patch,version-update:semver-minor
      merge-method: squash
