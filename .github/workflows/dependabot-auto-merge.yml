name: Dependabot Auto-merge

on:
  pull_request:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    name: Auto-merge patch/minor updates
    if: github.event.pull_request.head.repo.full_name == github.repository
    uses: dinglebear-ai/workflows/.github/workflows/dependabot-automerge.yml@d7bbe71ddc1157e32ed0bebf928fc07438ba58b0
    with:
      allowed-update-types: version-update:semver-patch,version-update:semver-minor
      merge-method: squash
