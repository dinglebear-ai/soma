name: Dependabot Auto-merge

on:
  pull_request:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    name: Auto-merge patch/minor updates
    if: github.event.pull_request.head.repo.full_name == github.repository
    uses: dinglebear-ai/workflows/.github/workflows/dependabot-automerge.yml@d1a41a7af9c41189e0f1062234364f5814bda99d
    with:
      allowed-update-types: version-update:semver-patch,version-update:semver-minor
      merge-method: squash
