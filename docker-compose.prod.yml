# =============================================================================
# docker-compose.prod.yml — Production deployment sample
#
# CUSTOMIZE: Replace all occurrences of "example" with your service name.
#           Replace all occurrences of "EXAMPLE" with your service name in caps.
#
# Key substitutions:
#   "soma"          -> your Docker service/container name
#   "soma-data"     -> your Docker volume name
#   "ghcr.io/.../example"  -> your published Docker image path
#   port 40060             -> your service's MCP port
#   SOMA_*              -> your env var prefix
# =============================================================================

name: soma

x-common-service: &common-service
  networks:
    - mcp
  restart: unless-stopped
  extra_hosts:
    - "host.docker.internal:host-gateway"
  ulimits:
    nofile:
      soft: 65535
      hard: 65535
  logging:
    driver: "json-file"
    options:
      max-size: "10m"
      max-file: "3"
      compress: "true"

services:
  soma:
    <<: *common-service
    # CUSTOMIZE: Replace with your image path.
    image: ghcr.io/your-org/soma:${SOMA_MCP_VERSION:-latest}
    container_name: soma
    env_file:
      - path: .env
        required: false
    environment:
      SOMA_HOME: /data
      SOMA_PROVIDER_DIR: /data/providers
      SOMA_PYTHON_RUNNER_MODE: persistent
      # Drop-in Python providers are trusted application code in this sample.
      # Brokered mode additionally requires delegated cgroup v2 configuration.
      SOMA_PYTHON_EXECUTION_PROFILE: trusted
      SOMA_MCP_HOST: 0.0.0.0
      # Local-runtime providers require a trusted transport boundary. Publish
      # only to host loopback and put an authenticating proxy in front.
      SOMA_NOAUTH: "true"
    ports:
      - "127.0.0.1:${SOMA_MCP_HOST_PORT:-40060}:40060/tcp"
    volumes:
      - ${HOME}/.soma:/data
      - ${HOME}/.soma/providers:/data/providers:ro
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    user: "1000:1000"
    read_only: true
    tmpfs:
      - /tmp
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:40060/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: '0.5'

volumes:
  soma-data:

networks:
  mcp:
    name: ${DOCKER_NETWORK:-mcp}
    external: true
